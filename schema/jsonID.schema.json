{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "id": "https://gitcdn.xyz/repo/jeffw387/VulkanSchema/master/schema/jsonID.schema.json",
    "type": "object",
    "properties": {
        "uri": {
            "type": "string",
            "minLength": 1,
            "description": "uri: A unique identifier per json instance"
        },
        "inline": {
            "type": "boolean",
            "description": "inline: if true, the data included with this uri should be inline,\notherwise it should be loaded using the uri"
        }
    },
    "required":[
        "uri",
        "inline"
    ]
}